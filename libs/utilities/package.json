{
  "name": "@em/utilities",
  "main": "src/index.js",
  "scripts": {
    "format": "prettier --cache --write .",
    "lint": "eslint ./src/**/*.js --fix",
    "test": "vitest run"
  },
  "devDependencies": {
    "@em/eslint-config": "workspace:*",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "vitest": "catalog:"
  }
}
